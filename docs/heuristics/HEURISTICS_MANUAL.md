# TaijiFlow AI - Heuristics Engine Manual v3.0

คู่มืออธิบายการทำงานของ Heuristics Engine และกฎการตรวจสอบท่าทาง Taijiquan

---

## 📖 Overview

**HeuristicsEngine** คือ "สมองกล" ของ TaijiFlow AI ที่ใช้กฎชุดนึง (Rule-Based) ในการวิเคราะห์ท่าทางผู้ใช้แบบ Real-time โดยเปรียบเทียบกับหลักการของมวยไท้เก๊ก

### Version History
- **v1.0** - กฎพื้นฐาน 8 ข้อ
- **v2.0** - เพิ่ม Dynamic Thresholds (Calibration)
- **v2.2** - เพิ่ม Sticky Feedback + Priority Ranking
- **v3.0** - เพิ่ม CONFIG object, Timestamps, Debug Mode

---

## 🎯 8 กฎการตรวจสอบ (Heuristic Rules)

### 1. Path Accuracy (ความแม่นยำของเส้นทาง)
**Purpose:** ตรวจสอบว่าข้อมือผู้ใช้เคลื่อนที่ตามเส้นทางต้นแบบหรือไม่

**Algorithm:**
```javascript
let minDistance = Infinity;
for (const refPoint of referencePath) {
  const d = calculateDistance(userWrist, refPoint);
  if (d < minDistance) minDistance = d;
}
// ถ้า minDistance > threshold → "เส้นทางไม่แม่นยำ"
```

**Threshold:** 
- Default: 0.08 (8% of screen)
- Calibrated: 40% of shoulderWidth
- Capped: min 0.02, max 0.25

**หลักการ:** ในมวยไท้เก๊ก การเคลื่อนที่ของแขนต้องเป็นวงกลมสม่ำเสมอ

---

### 2. Arm Rotation (การหมุนแขน)
**Purpose:** ตรวจสอบการหงาย/คว่ำฝ่ามือตามทิศทางการเคลื่อนที่

**กฎ:**
| Movement | rh_cw / lh_ccw | rh_ccw / lh_cw |
|----------|----------------|----------------|
| **ขึ้น** | หงาย (palm up) | คว่ำ |
| **ลง** | คว่ำ | หงาย |

**Algorithm:**
```javascript
// ตรวจจาก thumb.x vs pinky.x
const isRightHand = moveType.startsWith("rh");
const isSupinated = isRightHand 
  ? thumb.x > pinky.x  // นิ้วโป้งอยู่ขวา = หงาย
  : thumb.x < pinky.x; // นิ้วโป้งอยู่ซ้าย = หงาย
```

**หลักการ:** "หมุนข้อมือ" เป็นหลักสำคัญในการนำพลังผ่านแขน

---

### 3. Elbow Sinking (ศอกจม)
**Purpose:** ศอกต้องอยู่ต่ำกว่าไหล่เสมอ

**Algorithm:**
```javascript
// Image coords: Y เพิ่มลงล่าง
const tolerance = calibrationData.torsoHeight * 0.05;
if (elbow.y < shoulder.y - tolerance) {
  return "⚠️ ศอกลอย";
}
```

**หลักการ:** "จมศอก" (沉肘) ช่วยให้ไหล่ผ่อนคลายและพลังไหลลงดิน

---

### 4. Waist Initiation (เอวนำ)
**Purpose:** การเคลื่อนไหวต้องเริ่มจากเอว ไม่ใช่ไหล่

**Algorithm (v0.9.11):**
```javascript
const hipVel = getAngularVelocity(hipAngle1, hipAngle2, dt);
const shoulderVel = getAngularVelocity(shoulderAngle1, shoulderAngle2, dt);

// ถ้าไหล่หมุนเร็วกว่าเอว 2 เท่า → ไหล่นำ (ผิด)
if (hipVel > 1.0 && shoulderVel > hipVel * 2.0) {  // 🔄 was 2.0, 3.0
  return "⚠️ ใช้เอวนำ";
}
```

**หลักการ:** "เอวเป็นจักรเพลา" (腰为轴) ทุกการเคลื่อนไหวต้องเริ่มจากเอว

---

### 5. Vertical Stability (ความนิ่งของศีรษะ)
**Purpose:** ศีรษะต้องนิ่ง ไม่ขยับขึ้นลง

**Algorithm (v0.9.11 Time-Based):**
```javascript
// เก็บ nose.y ใน 5000ms window (time-based)
const now = Date.now();
const recentPoints = headYHistory.filter(p => p.t >= now - 5000);
const displacement = Math.max(...recentPoints.map(p => p.y)) 
                   - Math.min(...recentPoints.map(p => p.y));

// ถ้า displacement > 10% of torsoHeight → ศีรษะไม่นิ่ง
if (displacement > threshold) return "⚠️ ศีรษะไม่นิ่ง";
```

**หลักการ:** "โปรงกระหม่อมเบา" (虚领顶劲) ศีรษะต้องนิ่งเหมือนลอยน้ำ

---

### 6. Smoothness (ความลื่นไหล)
**Purpose:** การเคลื่อนไหวต้องสม่ำเสมอ ไม่สะดุด

**Algorithm:**
```javascript
// Time-normalized velocity
const v1 = dist(p1, p2) / dt1; // normalized units/sec
const v2 = dist(p2, p3) / dt2;
const acceleration = Math.abs(v2 - v1);

if (acceleration > threshold) return "⚠️ การเคลื่อนไหวสะดุด";
```

**Threshold:** 5% of armLength

**หลักการ:** "ต่อเนื่องไม่ขาดตอน" การเคลื่อนไหวต้องราวกับดึงไหม

---

### 7. Continuity (ความต่อเนื่อง)
**Purpose:** ไม่หยุดนิ่งระหว่างฝึก

**Algorithm (Time-Based v0.9.9):**
```javascript
// isPaused() - คำนวณ avg velocity ใน time window
const now = Date.now();
const windowStart = now - PAUSE_WINDOW_MS; // 2 seconds
const recentPoints = wristHistory.filter(p => p.t >= windowStart);
const avgVelocity = totalDistance / timeSpan;

// ถ้า avgVelocity < 0.003 → หยุดนิ่ง
if (avgVelocity < PAUSE_AVG_VELOCITY_THRESHOLD) {
  return "⚠️ เคลื่อนไหวต่อเนื่อง อย่าหยุดนิ่ง";
}
```

**หลักการ:** "เคลื่อนไหวไม่หยุด" พลังต้องไหลต่อเนื่อง

---

### 8. Weight Shift (การถ่ายน้ำหนัก)
**Purpose:** จุดศูนย์ถ่วงต้องอยู่ในฐานการยืน

**Algorithm (v0.9.11):**
```javascript
const hipCenter = (leftHip.x + rightHip.x) / 2;
const buffer = stanceWidth * 0.3;  // 🔄 was 0.1 (30% more sensitive)
const leftEdge = leftAnkle.x + buffer;
const rightEdge = rightAnkle.x - buffer;

if (hipCenter < leftEdge) return "⚠️ น้ำหนักเอียงซ้าย";
if (hipCenter > rightEdge) return "⚠️ น้ำหนักเอียงขวา";
```

**หลักการ:** "รากฝังลึก" ฐานต้องมั่นคง

---

## 🔧 Level Configuration

กฎที่ใช้ในแต่ละระดับ:

| Rule | L1 (นั่ง - 3 กฎ) | L2 (ยืน - 6 กฎ) | L3 (ยืนย่อ - 8 กฎ) |
|------|-----------------|-----------------|-------------------|
| 1. Path Accuracy | ✅ | ✅ | ✅ |
| 2. Arm Rotation | ❌ | ✅ | ✅ |
| 3. Elbow Sinking | ✅ | ✅ | ✅ |
| 4. Waist Initiation | ❌ | ✅ | ✅ |
| 5. Vertical Stability | ❌ | ✅ | ✅ |
| 6. Smoothness | ❌ | ❌ | ✅ |
| 7. Continuity | ✅ | ✅ | ✅ |
| 8. Weight Shift | ❌ | ❌ | ✅ |

---

## 📊 Priority Ranking

เมื่อตรวจพบหลายข้อผิดพลาด จะแสดงเฉพาะข้อที่สำคัญที่สุด:

| Priority | Rule | เหตุผล |
|----------|------|--------|
| 1 | Path Accuracy | รูปทรงผิด ต้องแก้ก่อน |
| 2 | Waist Initiation | แกนกลางสำคัญ |
| 3 | Weight Shift | ฐานราก |
| 4 | Vertical Stability | ความสมดุล |
| 5 | Arm Rotation | เทคนิคแขน |
| 6 | Elbow Sinking | เทคนิคศอก |
| 7 | Smoothness | คุณภาพการเคลื่อน |
| 8 | Continuity | ความต่อเนื่อง |

---

## 🐛 Debug Mode

กด **`D`** ระหว่างฝึกเพื่อดู:
- `pathDistance` / `pathThreshold`
- `wristVelocity` / `acceleration`

จะแสดงกล่อง cyan ที่มุมขวาบนของวิดีโอ
