### **📄 3.2.2 Use Case Description: Perform Training**

Use Case ID: UC-02

Use Case Name: Perform Training (ฝึกฝนท่าม้วนไหม)

Actor(s): Trainee (ผู้ฝึก)

Description: ผู้ใช้งานทำการฝึกฝนท่าม้วนไหม โดยระบบจะตรวจจับท่าทางและให้คำแนะนำแบบ Real-time

Trigger: ผู้ใช้งานกดเลือกท่าฝึกและระดับความยาก

Precondition(s): ผู้ใช้งานต้องทำการปรับเทียบสัดส่วน (Calibrate) เรียบร้อยแล้ว (หรือระบบบังคับให้ทำก่อนเริ่ม)

---

#### **ตาราง Use Case Input Specification**

*(อ้างอิงข้อมูลตัวแปรจาก script.js และ heuristics\_engine.js)*

| Input | Data Type | Constraint | Example |
| ----- | :---: | ----- | ----- |
| currentExercise | STRING | ค่าต้องไม่เป็นค่าว่าง (Select from list) | "rh\_cw" (มือขวา-ตามเข็ม) |
| currentLevel | STRING | ต้องเป็นค่า 'L1', 'L2', หรือ 'L3' | "L1" (ท่านั่ง) |
| video\_feed | STREAM | ต้องมีความละเอียดขั้นต่ำตามที่กำหนด | 1280x720 @ 30fps |
| pose\_landmarks | OBJECT ARRAY | อาเรย์พิกัด 33 จุด (x, y, z, visibility) | {x: 0.5, y: 0.5, z: 0, visibility: 0.9} |

**Post-condition(s):**

* คะแนนและข้อมูลการฝึก (Raw Keypoints) ถูกบันทึกลงในหน่วยความจำชั่วคราวเพื่อรอการสรุปผล

---

#### **ตาราง Normal Flows / Alternative / Exception**

| Actor (Trainee) | System (TaijiFlow AI) |
| ----- | ----- |
| 1\. ผู้ใช้งานเลือก "ท่าฝึก" และ "ระดับความยาก" จากเมนูหน้าหลัก | 2\. ระบบตรวจสอบสิทธิ์การเข้าถึงกล้องและเริ่มต้นการทำงานของกล้อง |
|  | 3\. ระบบโหลดข้อมูลท่าทางต้นแบบ (Reference Data) จากไฟล์ JSON ที่ตรงกับท่าที่เลือก |
|  | 4\. ระบบแสดงผลหน้าจอการฝึก ประกอบด้วย: \- ภาพวิดีโอจากกล้องแบบกลับด้าน (Mirrored) \- เส้นโครงร่างร่างกาย (Skeleton) \- เส้นนำทาง (Reference Path) |
| 5\. ผู้ใช้งานเริ่มขยับร่างกายตามเส้นนำทาง | 6\. ระบบตรวจจับพิกัดร่างกาย (Pose Landmarks) จำนวน 33 จุด ด้วย MediaPipe แบบ Real-time |
|  | 7\. **(Heuristics Analysis)** ระบบคำนวณความถูกต้องตามกฎ 8 ข้อ (เช่น Path Accuracy, Waist Initiation) โดยเปรียบเทียบกับค่า Threshold ที่ได้จากการ Calibration |
|  | 8\. ระบบแสดงผลตอบกลับ (Feedback): \- แสดงข้อความแจ้งเตือน (Text Alert) และเปลี่ยนสีเส้นโครงร่าง \- ส่งเสียงแจ้งเตือน (Audio Cue) |
|  | 9\. ระบบนับจำนวนเฟรมที่ถูกต้อง/ผิดพลาด เพื่อนำไปคำนวณคะแนน |
| 10\. ผู้ใช้งานกดปุ่ม "หยุดการฝึก" (Stop) หรือหมดเวลาการฝึก (5 นาที) | 11\. ระบบจบการทำงานในรอบนั้น และนำผู้ใช้ไปสู่หน้าสรุปผล (UC-03) |

---

**Alternative Flow (ทางเลือก):**

* **\[A1: สั่งงานด้วยท่าทาง (Gesture Control)\]**  
  * ในขั้นตอนที่ 1 หรือ 9 ผู้ใช้งานสามารถใช้ท่าทางมือ (ยกนิ้วโป้ง 👍 / กำมือ ✊) ค้างไว้ 2 วินาที แทนการกดปุ่มบนหน้าจอเพื่อเริ่มหรือหยุดการฝึกได้ \[FR-11\]  
* **\[A2: ขยายเต็มจอ (Fullscreen)\]**  
  * ผู้ใช้สามารถกดปุ่ม Fullscreen หรือกดปุ่ม F เพื่อขยายหน้าจอได้ตลอดการฝึก

**Exception Flow (ข้อยกเว้น):**

* **\[E1: กล้องไม่ทำงาน (Camera Error)\]**  
  * หาก Browser ไม่สามารถเข้าถึงกล้องได้ ระบบจะแสดง Popup แจ้งเตือนให้ผู้ใช้ตรวจสอบสิทธิ์การเข้าถึง (Permission)  
* **\[E2: ไม่พบข้อมูลต้นแบบ (Reference Data Missing)\]**  
  * หากระบบไม่สามารถโหลดไฟล์ JSON ของท่านั้นได้ ระบบจะแสดงข้อความแจ้งเตือน "ไม่พบข้อมูลต้นแบบ" และไม่แสดงเส้นนำทาง

---

**Assumption (ข้อสมมติฐาน):**

* ผู้ใช้งานมีกล้อง Webcam ที่ใช้งานได้และมีแสงสว่างเพียงพอ  
* ผู้ใช้งานยืนห่างจากกล้องในระยะที่ระบบมองเห็นได้ทั้งตัว (Full body visibility)

