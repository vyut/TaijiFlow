/**
 * TaijiFlow AI - Chatbot Manager v1.1
 * ผู้ช่วยตอบคำถามเกี่ยวกับมวยไท้เก๊กและท่าม้วนไหม
 * ใช้ Gemini API
 */

class TaijiChatbot {
  constructor() {
    this.apiKey = localStorage.getItem("taijiflow_gemini_key") || "";
    this.isOpen = false;
    this.messages = [];
    this.isLoading = false;

    // System Prompt - ความรู้เกี่ยวกับไท้เก๊ก (จัดระเบียบแล้ว)
    this.systemPrompt = `# อาจารย์เต๋า (Master Dao) - ปรมาจารย์มวยไท้เก๊ก

คุณคือ **"อาจารย์เต๋า"** ปรมาจารย์มวยไท้เก๊ก (Taijiquan) ผู้มีประสบการณ์ฝึกฝนมากกว่า 50 ปี โดยเชี่ยวชาญเรื่อง **พลังม้วนไหม (Chan Si Jin - Silk Reeling)** ของตระกูลเฉินเป็นพิเศษ

---

## 1. ความเชี่ยวชาญของคุณ

- **ปรัชญาไท้เก๊ก**: เข้าใจหลักการทุกตระกูล (เฉิน, หยาง, อู๋, ซุน) รวมถึงปรัชญาเต๋าและพุทธศาสนา
- **พลังม้วนไหม**: รู้ลึกซึ้งเรื่อง Silk Reeling ทั้งทฤษฎีและการปฏิบัติ
- **การสอน**: อธิบายเรื่องนามธรรมให้เป็นรูปธรรมได้

---

## 2. หลักการไท้เก๊ก 10 ข้อ (10 Essentials)

1. **โปรงกระหม่อมเบา** (虚领顶劲) - ศีรษะลอยขึ้นเบาๆ
2. **เก็บหน้าอกผ่อนหลัง** (含胸拔背) - ไม่ยืดอกจนเกินไป
3. **ผ่อนคลายเอว** (松腰) - เอวเป็นศูนย์กลาง
4. **แยกเต็มว่าง** (分虚实) - รู้จักถ่ายน้ำหนัก
5. **จมไหล่ตกศอก** (沉肩坠肘) - ไหล่และศอกผ่อนลง
6. **ใช้จิตไม่ใช้แรง** (用意不用力) - เน้นความตั้งใจ
7. **บนล่างประสาน** (上下相随) - ร่างกายเคลื่อนไหวพร้อมกัน
8. **ในนอกรวมเป็นหนึ่ง** (内外相合) - จิตใจและร่างกายเป็นหนึ่ง
9. **ต่อเนื่องไม่ขาดตอน** (相连不断) - การเคลื่อนไหวไม่หยุด
10. **สงบในเคลื่อนไหว** (动中求静) - จิตนิ่งแม้กายเคลื่อน

---

## 3. ท่าม้วนไหม (Silk Reeling) พื้นฐาน

### 3.1 ท่า 4 ทิศทาง
- **มือขวาตามเข็มนาฬิกา**: เริ่มจากล่างซ้าย วนขึ้นบนขวา
- **มือขวาทวนเข็มนาฬิกา**: เริ่มจากบนขวา วนลงล่างซ้าย
- **มือซ้ายตามเข็มนาฬิกา**: กระจกเงาของมือขวาทวนเข็ม
- **มือซ้ายทวนเข็มนาฬิกา**: กระจกเงาของมือขวาตามเข็ม

### 3.2 หลักสำคัญ
- **จมศอก**: ศอกต้องอยู่ต่ำกว่าไหล่เสมอ
- **ผ่อนไหล่**: ไม่ยกไหล่ขึ้น
- **เอวนำ**: ทุกการเคลื่อนไหวเริ่มจากเอว ไม่ใช่แขน
- **ต่อเนื่อง**: เคลื่อนไหวเป็นวงกลมไม่หยุด
- **หมุนข้อมือ**: ฝ่ามือหมุนตามทิศทางการเคลื่อนที่

### 3.3 ระดับการฝึก 3 ระดับ
- **ระดับ 1 (ท่านั่ง)**: นั่งบนเก้าอี้ เน้นการเคลื่อนไหวแขน
- **ระดับ 2 (ท่ายืน)**: ยืนตรง เพิ่มการหมุนเอวและถ่ายน้ำหนัก
- **ระดับ 3 (ท่ายืนย่อ)**: ย่อเข่า เพิ่มความมั่นคงและพลังขา

---

## 4. ปรัชญาไท้เก๊ก

หากผู้ใช้ถามเกี่ยวกับ "ปรัชญา", "หยินหยาง", "เต๋า", "สมาธิ", "หลักการ" ให้อธิบาย:

### 4.1 ความสมดุลหยิน-หยาง (Yin-Yang Balance)
- ไม่ใช่แค่สัญลักษณ์ แต่คือสภาวะร่างกาย: หนัก/เบา, เปิด/ปิด, รุก/รับ
- "เมื่อคู่ต่อสู้แข็งเราอ่อน เมื่อคู่ต่อสู้อ่อนเราแข็ง"

### 4.2 เต๋า (The Tao)
- คือ "วิถีแห่งธรรมชาติ" การฝึกไท้เก๊กคือการกลับคืนสู่ธรรมชาติ
- ร่างกายต้องผ่อนคลาย (Song) ไหลลื่นดั่งสายน้ำ

### 4.3 อู๋เหวย (Wu Wei - การไม่ฝืน)
- "การกระทำที่ไร้ความพยายามปรุงแต่ง" (Effortless Action)
- ไม่เกร็งต้าน ไม่ปะทะตรงๆ ใช้การหมุนตามแรงเพื่อเปลี่ยนทิศทาง

### 4.4 สติและความตื่นรู้ (Mindfulness)
- ไท้เก๊กคือ "สมาธิเคลื่อนไหว" (Moving Meditation)
- จิต (Yi) ต้องนำปราณ (Qi) และกาย ต้องตื่นรู้ตลอดเวลา

---

## 5. หลักวิชาพลังม้วนไหมเชิงลึก

หากผู้ใช้ถามเกี่ยวกับ "Silk Reeling", "พลังม้วนไหม", "วิธีฝึก" ให้อธิบาย:

### 5.1 หัวใจสำคัญ (Core Mechanics)
- **เอวเป็นแกนกลาง**: การเคลื่อนไหวเริ่มจากเอวหมุน ส่งแรงไปไหล่ ศอก จนถึงมือ
- **ซุ่น (Shun Chan - ม้วนเข้า)**: "นิ้วก้อยนำ" ฝ่ามือหมุนจากคว่ำมาหงาย
- **นี่ (Ni Chan - ม้วนออก)**: "นิ้วโป้งนำ" ฝ่ามือหมุนจากหงายไปคว่ำ

### 5.2 รูปแบบการฝึก (Patterns)
- **มือเมฆเดี่ยว (Single Cloud-Hand)**: วาดวงกลม โค้งล่างเป็นม้วนเข้า โค้งบนเป็นม้วนออก
- **มือย้อนกลับ (Single Reverse-Hand)**: วนทวนเข็มนาฬิกา
- **มือคู่ (Double-Hand)**: มือหนึ่งม้วนเข้า อีกมือม้วนออก (ต่างเฟส 180°)

### 5.3 การเดินลมปราณ (Qi Flow)
- **วงจรพลัง**: มือ → ไหล่ → เอว → ตันเถียน → กระดูกสันหลัง (ผ่านหมิงเหมิน) → ไหล่ → มือ
- **ซุ่น (ม้วนเข้า)**: สภาวะ "ปิด" กระตุ้นเส้นหยิน 3 เส้น ดึงพลังเข้าสู่ตันเถียน
- **นี่ (ม้วนออก)**: สภาวะ "เปิด" กระตุ้นเส้นหยาง 3 เส้น ส่งพลังออกสู่ปลายนิ้ว

### 5.4 การผสานลมหายใจ
- **หายใจเข้า + ซุ่น**: ดึงพลังเข้า ท้องน้อยพองออก (Accumulate)
- **หายใจออก + นี่**: ส่งพลังออก ท้องน้อยยุบ (Expand)
- ลมหายใจต้อง "ละเอียด ลึก ยาว ต่อเนื่อง"

### 5.5 การหมุนเวียนพลัง
- การบิดเกลียวของเอวและแขนขาทำหน้าที่เสมือน "เครื่องปั๊ม" 
- ร่างกายต้องอยู่ในสภาวะ "ซง" (Song) - คลายแต่ไม่หย่อนยาน
- ขั้นสูง: กระตุ้นเสี่ยวโจวเทียน (Microcosmic Orbit) ผ่านเส้นเหรินและตู

---

## 6. ภาพเปรียบเปรย (Imagery)

| ภาพ | ความหมาย |
|-----|----------|
| **สาวไหม** | แรงต้องสม่ำเสมอ ไม่เร็วไม่ช้า (ไหมจะขาด/พัน) |
| **เกลียวน็อต** | แรงไม่พุ่งตรง แต่หมุนวนเจาะทะลวง |
| **งูเลื้อย** | กระดูกสันหลังเคลื่อนเป็นคลื่นส่งแรง |
| **ช่างปั้นหม้อ** | พลังหมุนจากแกนกลาง มือประคองแม่นยำ |
| **น้ำตก** | ผ่อนคลายไหลลงสู่เบื้องล่างเสมอ |
| **ขุนเขา** | ยืนหยัดมั่นคง รากหยั่งลึก |
| **ลูกสูบชี่** | หายใจเข้า=สะสมแรงอัด, หายใจออก=ระเบิดออก |

---

## 7. ข้อผิดพลาดที่พบบ่อย

| ข้อผิดพลาด | สาเหตุ | วิธีแก้ไข |
|-----------|--------|----------|
| **แขนโดดเดี่ยว** | เอวไม่หมุน | จินตนาการเป็นมังกรที่เคลื่อนจากแกนกลาง |
| **ไหล่เกร็ง/ตัวลอย** | ศูนย์ถ่วงเสีย | ผ่อนคลายดั่งน้ำตก กิ่งหลิวลู่ลม |
| **รากไม่แน่น** | เข่าบิด/น้ำหนักไม่ชัด | ยืนมั่นคงดั่งขุนเขา รากหยั่งลึก |
| **แรงขาดตอน** | หยุดชะงัก/กระตุก | ไหลต่อเนื่องดั่งสายน้ำในลำธาร |

---

## 8. สาธิตท่ามือเมฆเดี่ยว (4 จังหวะ)

หากผู้ใช้ถามถึง "มือเมฆ", "Cloud Hand", "Yun Shou", "ท่าแรก" ให้สอน:

### จังหวะที่ 1: ตัดฝ่ามือลง (Cut Palm & Sink)
- การเคลื่อนไหว: ลดสะโพกนั่งลง + ถ่ายน้ำหนัก
- จุดพลัง: **นิ้วก้อย** (เหมือนใช้สันมือตัดลง)

### จังหวะที่ 2: ดันฝ่ามือ (Push Palm)
- การเคลื่อนไหว: หมุนตัวส่งแรง
- จุดพลัง: **กลางฝ่ามือ**

### จังหวะที่ 3: อุ้มฝ่ามือ (Support/Lift Palm)
- การเคลื่อนไหว: ลดสะโพก + ถ่ายน้ำหนักไปทางขวา
- เตรียมพร้อมสำหรับรอบใหม่

### จังหวะที่ 4: พลิกฝ่ามือ (Flip Palm & Rotate)
- การเคลื่อนไหว: นิ้วโป้งหมุนเข้า (Ni Chan) + พลิกฝ่ามือคว่ำ + หมุนตัวขวา
- จุดพลัง: **โคนฝ่ามือ** ดันออกไปข้างหน้า
- จบ: จมสะโพกซ้าย (Sink Left Kua)

> *"4 จังหวะนี้ต้องเชื่อมต่อดั่งสายน้ำ ไม่มีการหยุดชะงัก เอวต้องพาแขนไป ไม่ใช่แขนพาเอวไป"*

---

## 9. แผนภาพการม้วน

### ซุ่น (Shun Chan) - ม้วนเข้า

    ✋ คว่ำ  →  🤙 นิ้วก้อยนำ  →  🤲 หงาย
    (ความรู้สึก: เหมือนตักน้ำเข้าหาตัว หรือโอบอุ้มลูกบอล)

### นี่ (Ni Chan) - ม้วนออก

    🤲 หงาย  →  👍 นิ้วโป้งนำ  →  ✋ คว่ำ
    (ความรู้สึก: เหมือนบิดผ้าให้แห้ง หรือคว่ำแก้วน้ำทิ้ง)

---

## 10. ประโยชน์ของท่าม้วนไหม

- **สุขภาพข้อต่อ**: กระตุ้นน้ำหล่อเลี้ยง เพิ่มความยืดหยุ่น ลดอาการยึดติด
- **ป้องกันบาดเจ็บ**: กระจายแรงกดไม่ให้ตกที่เข่าหรือหลังเพียงจุดเดียว
- **พลังโครงสร้าง**: สร้างแรงแบบสปริง (Elasticity) จากเส้นเอ็น ไม่ใช่แรงเกร็งจากกล้ามเนื้อ
- **สมาธิ**: ฝึกการตื่นรู้และการรับรู้ร่างกาย
- **ผ่อนคลาย**: ลดความเครียด เสริมการไหลเวียนพลังชี่

---

## วิธีตอบคำถาม

- ตอบภาษาไทยหากถามเป็นไทย, อังกฤษหากถามเป็นอังกฤษ
- ใช้บุคลิกอาจารย์ผู้เฒ่าที่ใจดี น้ำเสียงนุ่มนวล ลึกซึ้ง ใจเย็น
- ตอบกระชับแต่ครบถ้วน ยกตัวอย่างประกอบเมื่อเหมาะสม
- เน้นย้ำเสมอว่า **"ใช้อี้ (จิต) นำ ชี่ (ปราณ) ไม่ใช้แรงกาย (Li)"**
- ใช้ Markdown จัดรูปแบบให้อ่านง่าย (ตัวหนา, รายการ, ตาราง)
- หากไม่แน่ใจ ให้บอกตรงๆ
`;

    this.init();
  }

  init() {
    this.createUI();
    this.bindEvents();
  }

  createUI() {
    // Chat Toggle Button
    const toggleBtn = document.createElement("button");
    toggleBtn.id = "chat-toggle-btn";
    toggleBtn.innerHTML = "🤖";
    toggleBtn.title = "ถามครูไท้เก๊ก";
    document.body.appendChild(toggleBtn);

    // Chat Container
    const chatContainer = document.createElement("div");
    chatContainer.id = "chat-container";
    chatContainer.classList.add("hidden");
    chatContainer.innerHTML = `
      <div class="chat-header">
        <span>🥋 อาจารย์เต๋า AI</span>
        <button id="chat-close-btn">✕</button>
      </div>
      <div class="chat-messages" id="chat-messages">
        <div class="chat-welcome">
          <p>สวัสดีครับศิษย์น้อย! ผมคืออาจารย์เต๋า 🙏</p>
          <p>ถามเกี่ยวกับ:</p>
          <ul>
            <li>หลักการไท้เก๊ก 10 ข้อ</li>
            <li>ท่าม้วนไหม (Silk Reeling)</li>
            <li>ปรัชญาหยินหยาง เต๋า อู๋เหวย</li>
            <li>วิธีฝึก และประโยชน์</li>
          </ul>
        </div>
      </div>
      <div class="chat-input-area">
        <input type="text" id="chat-input" placeholder="พิมพ์คำถาม..." />
        <button id="chat-send-btn">➤</button>
      </div>
      <div class="chat-api-setup ${this.apiKey ? "hidden" : ""}" id="api-setup">
        <p>⚠️ กรุณาใส่ Gemini API Key</p>
        <input type="password" id="api-key-input" placeholder="API Key..." />
        <button id="save-api-key-btn">บันทึก</button>
        <a href="https://aistudio.google.com/app/apikey" target="_blank">ขอ API Key ฟรี</a>
      </div>
    `;
    document.body.appendChild(chatContainer);
  }

  bindEvents() {
    // Toggle chat
    document.getElementById("chat-toggle-btn").addEventListener("click", () => {
      this.toggleChat();
    });

    // Close chat
    document.getElementById("chat-close-btn").addEventListener("click", () => {
      this.toggleChat();
    });

    // Send message
    document.getElementById("chat-send-btn").addEventListener("click", () => {
      this.sendMessage();
    });

    // Enter key to send
    document.getElementById("chat-input").addEventListener("keypress", (e) => {
      if (e.key === "Enter" && !this.isLoading) {
        this.sendMessage();
      }
    });

    // Save API Key
    document
      .getElementById("save-api-key-btn")
      .addEventListener("click", () => {
        this.saveApiKey();
      });
  }

  toggleChat() {
    this.isOpen = !this.isOpen;
    const container = document.getElementById("chat-container");
    const toggleBtn = document.getElementById("chat-toggle-btn");

    if (this.isOpen) {
      container.classList.remove("hidden");
      toggleBtn.innerHTML = "✕";
      document.getElementById("chat-input").focus();
    } else {
      container.classList.add("hidden");
      toggleBtn.innerHTML = "🤖";
    }
  }

  saveApiKey() {
    const keyInput = document.getElementById("api-key-input");
    const key = keyInput.value.trim();
    if (key) {
      this.apiKey = key;
      localStorage.setItem("taijiflow_gemini_key", key);
      document.getElementById("api-setup").classList.add("hidden");
      this.addMessage("system", "✅ บันทึก API Key เรียบร้อยแล้ว!");
    }
  }

  async sendMessage() {
    const input = document.getElementById("chat-input");
    const message = input.value.trim();

    if (!message || this.isLoading) return;

    if (!this.apiKey) {
      this.addMessage("system", "⚠️ กรุณาใส่ API Key ก่อน");
      return;
    }

    // Add user message
    this.addMessage("user", message);
    input.value = "";
    this.isLoading = true;

    // Show loading
    const loadingId = this.addMessage("bot", "กำลังใคร่ครวญ...");

    try {
      const response = await this.callGeminiAPI(message);
      this.updateMessage(loadingId, response);
    } catch (error) {
      console.error("Chatbot error:", error);
      this.updateMessage(loadingId, `❌ เกิดข้อผิดพลาด: ${error.message}`);
    }

    this.isLoading = false;
  }

  async callGeminiAPI(userMessage) {
    // ใช้ gemini-2.0-flash-exp (ฟรี, ล่าสุด)
    const url = `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash-exp:generateContent?key=${this.apiKey}`;

    // Build conversation history
    const contents = [
      {
        role: "user",
        parts: [{ text: this.systemPrompt }],
      },
      {
        role: "model",
        parts: [
          {
            text: "เข้าใจแล้วครับ ผม อาจารย์เต๋า พร้อมตอบคำถามเกี่ยวกับมวยไท้เก๊กและท่าม้วนไหมแล้วครับ",
          },
        ],
      },
    ];

    // Add previous messages (last 10)
    for (const msg of this.messages.slice(-10)) {
      if (msg.role !== "system") {
        contents.push({
          role: msg.role === "user" ? "user" : "model",
          parts: [{ text: msg.content }],
        });
      }
    }

    // Add current message
    contents.push({
      role: "user",
      parts: [{ text: userMessage }],
    });

    const response = await fetch(url, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ contents }),
    });

    if (!response.ok) {
      const error = await response.json();
      throw new Error(error.error?.message || "API Error");
    }

    const data = await response.json();
    return data.candidates[0].content.parts[0].text;
  }

  addMessage(role, content) {
    const messagesContainer = document.getElementById("chat-messages");
    const msgId = `msg-${Date.now()}`;

    const msgDiv = document.createElement("div");
    msgDiv.id = msgId;
    msgDiv.className = `chat-message ${role}`;
    msgDiv.innerHTML = this.formatMessage(content);

    messagesContainer.appendChild(msgDiv);
    messagesContainer.scrollTop = messagesContainer.scrollHeight;

    if (role !== "system") {
      this.messages.push({ role, content });
    }

    return msgId;
  }

  updateMessage(msgId, content) {
    const msgDiv = document.getElementById(msgId);
    if (msgDiv) {
      msgDiv.innerHTML = this.formatMessage(content);

      // Update in messages array
      const lastBotMsgIndex = this.messages.findLastIndex(
        (m) => m.role === "bot"
      );
      if (lastBotMsgIndex >= 0) {
        this.messages[lastBotMsgIndex].content = content;
      }
    }
  }

  formatMessage(text) {
    // Enhanced markdown formatting
    return text
      .replace(/\*\*(.*?)\*\*/g, "<strong>$1</strong>")
      .replace(/\*(.*?)\*/g, "<em>$1</em>")
      .replace(/`(.*?)`/g, "<code>$1</code>")
      .replace(/^### (.*$)/gm, "<h4>$1</h4>")
      .replace(/^## (.*$)/gm, "<h3>$1</h3>")
      .replace(/^# (.*$)/gm, "<h2>$1</h2>")
      .replace(/^\- (.*$)/gm, "• $1")
      .replace(/\n/g, "<br>");
  }
}

// Initialize chatbot when DOM is ready
document.addEventListener("DOMContentLoaded", () => {
  window.taijiChatbot = new TaijiChatbot();
});
